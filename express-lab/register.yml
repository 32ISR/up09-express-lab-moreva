info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}api/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "morelllin",
        "password": "makitra100"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
