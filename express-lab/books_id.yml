info:
  name: books_id
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}api/books/:id"
  params:
    - name: id
      value: "5"
      type: path
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
