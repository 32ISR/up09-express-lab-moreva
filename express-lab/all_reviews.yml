info:
  name: all_reviews
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}api/books/:id/reviews\t"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
